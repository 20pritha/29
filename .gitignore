# dependencies
node_modules/

# account database (contains password hashes — must never be committed)
server/users.db
server/users.db-shm
server/users.db-wal
server/users.json

# local noise
.DS_Store
*.log

# Firebase service-account key (SECRET — never commit)
firebase-service-account.json
server/firebase-service-account.json
*.serviceaccount.json
