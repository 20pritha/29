// Optional deploy config.
// Leave empty when the Node server also serves this site (the normal case).
// If you host the front end somewhere that can't run a WebSocket server (Vercel,
// Netlify, GitHub Pages), point this at your game server:
//   window.TWENTYNINE_SERVER = 'my-app.onrender.com';
window.TWENTYNINE_SERVER = '';
