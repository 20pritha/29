// Deploy config.
//
// TWENTYNINE_SERVER = your game server's host. It is used only when the page is
// served from somewhere OTHER than that server (e.g. a Vercel/Netlify front end,
// or a file:// open). When you open the game straight from the server's own URL,
// or on localhost during development, this is ignored and the same origin is used.
//
// Point it at your Render backend so the app connects no matter where the page
// itself is hosted. Update it if you rename the service or move hosts.
window.TWENTYNINE_SERVER = 'two9-yhix.onrender.com';
